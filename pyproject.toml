[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[project]
name = "agentgrunt"
version = "0.1.6"
description = "Agent Grunt"
authors = ["jeremiah-k <your@email.com>"]
license = "MIT"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
  "fastapi",
  "httpx",
  "plumbum",
  "pydantic",
  "pytest",
  "tqdm",
  "typer",
]

[project.scripts]
agentgrunt = "agentgrunt.cli:app"

[tool.trunk]
enabled = true

[tool.coverage.run]
branch = true
source = ["agentgrunt"]

[tool.coverage.report]
show_missing = true
skip_covered = true
