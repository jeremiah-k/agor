# AGOR CLI Dependencies - For local AGOR installation and development
# NOTE: AI agents should NOT install these dependencies
# Agents only need: pip install pydantic pydantic-settings
# These dependencies are for the AGOR CLI tool, not for agent dev tooling

fastapi
httpx
plumbum
pydantic
pyperclip
pytest
tqdm
typer
platformdirs>=4.3.7,<5.0.0
